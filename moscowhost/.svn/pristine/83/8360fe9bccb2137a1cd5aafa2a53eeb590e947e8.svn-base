DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',  # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': 'moscowhost',  # Or path to database file if using sqlite3.
        # The following settings are not used with sqlite3:
        #'USER': 'ebs',
        #'PASSWORD': '1234',
        #'HOST': '192.168.0.214',  # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
        'USER':'postgres',
        'PASSWORD':'1',
        'HOST': 'localhost',
        'PORT': '5432',  # Set to empty string for default.
    }
}

CURRENT_HOST = '192.168.0.228:80'

ALLOWED_HOSTS = [
    '127.0.0.1',  # Allow domain and subdomains
    '127.0.0.1.',  # Also allow FQDN and subdomains
]

DEBUG = True
DEBUG_SQL = False
